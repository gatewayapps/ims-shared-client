import React from 'react'

export const RequiredIndicator = (props) => (
  <i className='fa fa-asterisk text-danger' style={{ fontSize: '8pt' }} aria-label='Required' />
)

export default RequiredIndicator
